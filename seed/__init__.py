"""my_erp seed package — story-driven 18-month dataset generator.

Run via:

    python -m seed.seed [--dry-run] [--stop-after STEP]

from the project root, or:

    python seed/seed.py [...]

— the script bootstraps the package onto sys.path either way.
"""
